## 7-字符串

	1. 最长不含重复字符的子字符串
	2. 242.有效的字母异位词
	3. 22.括号生成
	4. 557.反转字符串中的单词 III
	5. 165.比较版本号
	6. 剑指 Offer 48. 最长不含重复字符的子字符串
	7. 680.验证回文字符串 Ⅱ
	8. 9.回文数


​		
​	

### 1. 最长不含重复字符的子字符串

请从字符串中找出一个最长的不包含重复字符的子字符串，计算该最长子字符串的长度。

 

示例 1:

输入: "abcabcbb"
输出: 3 
解释: 因为无重复字符的最长子串是 "abc"，所以其长度为 3。
示例 2:

输入: "bbbbb"
输出: 1
解释: 因为无重复字符的最长子串是 "b"，所以其长度为 1。
示例 3:

输入: "pwwkew"
输出: 3
解释: 因为无重复字符的最长子串是 "wke"，所以其长度为 3。
     请注意，你的答案必须是 子串 的长度，"pwke" 是一个子序列，不是子串。

 

### 第一遍写法：

	思路：遍历s
	起始条件，low = 0, high = 1
	word = (low, high)
	如果 high 字符存在，low 左移到 map[high]
	使用map存储字母的下一个位置

 



 可以优化的地方:   

 查找 字符是否存在，可以使用 map.has  

 不需要保存 word，我们只需要 high 和 low 即可  

 初始化，不一定high 需要从 1 开始

```javascript
var lengthOfLongestSubstring = function(s) {
    if(s.length <= 1) {
        return s.length
    }
    let low = 0;
    let high = 1;
    let word = [s.charAt(0)];
    let maxLength = word.length;
    let map = new Map();
    map.set(s.charAt(0), 1);
    while(high < s.length) {
        let cur = s.charAt(high);
        let index = word.indexOf(cur);
        if(index > -1) {
            low = map.get(cur);
        }
        map.set(cur, ++high);
        word = s.substring(low, high);
        maxLength = Math.max(maxLength, word.length);
    }
    return maxLength
};
```

### 改良后的map:



```js
var lengthOfLongestSubstring = function(s) {
    if(s.length <= 1) {
        return s.length
    }
    let low = 0;
    let high = 0;
    let maxLength = 0;
    let map = new Map();
    while(high < s.length) {
        let cur = s[high];
        if(map.has(cur)){
            low = map.get(cur)
        }
        map.set(cur, ++high);
        maxLength = Math.max(maxLength, high - low);
    }
    return maxLength
};


```



### 2. 有效的字母异位词


给定两个字符串 s 和 t ，编写一个函数来判断 t 是否是 s 的字母异位词。

示例 1:

	输入: s = "anagram", t = "nagaram"
	输出: true
示例 2:

	输入: s = "rat", t = "car"
	输出: false


​	

### 解题思路

方法一：先将两个字符串转成数组，然后排序，比较是否相同

时间复杂度：O(nlogn)

方法二：统计字符串中每个单词出现的次数，看两个字符串，每个字母出现的次数是否相同

时间复杂度：O(n)

空间复杂度 O(1) O(26)==O(1) 表大小不变复杂性不变


### 方法一

```javascript
var isAnagram = function(s, t) {
    let s_new = Array.from(s).sort().toString();
    let t_new = Array.from(t).sort().toString();
    return s_new == t_new;
};

```
### 方法一

```javascript
var isAnagram = function(s, t) {
   return s.split('').sort().join('') === t.split('').sort().join('')
};
```


### 方法二

```javascript
/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var isAnagram = function(s, t) {
    let map = new Map();
    for(let i of s) {
        if(!map.has(i)) {
            map.set(i, 1)
        }
        else {
            map.set(i, map.get(i) + 1)
        }
    }
    for(let j of t) {
        let num = map.get(j);
        if(!num) {
            return false
        }
        else if(num > 1) {
            map.set(j, num - 1)
        }
        else if(num == 1) {
            map.delete(j)
        }
    }
    return map.size == 0
};
```


### 3. 22. 括号生成

数字 n 代表生成括号的对数，请你设计一个函数，用于能够生成所有可能的并且 有效的 括号组合。

 

示例：

	输入：n = 3
	输出：[
	       "((()))",
	       "(()())",
	       "(())()",
	       "()(())",
	       "()()()"
	     ]

### 解题思路
![img](https://pic.leetcode-cn.com/1f18f8cae809764acdeca72f8049a8fca15e1c462643273063bb19bc2f166512-QQ%E5%9B%BE%E7%89%8720200409115535.png)

什么情况下可以添加添加左括号呢？

最多能添加 n 个左括号，在递归调用的时候，在能传递到最底层的共用字符串中先添加 ”(“ ，然后 left-1，递归调用就可以。

什么时候添加右括号呢？

当左括号个数大于右括号的个数时添加右括号。

1. 插入数量不超过n
2. 可以插入 ） 的前提是 ( 的数量大于 ）

### 代码

```javascript
/**
 * @param {number} n
 * @return {string[]}
 */
var generateParenthesis = function(n) {
    let left = n;
    let right = n;
    let result = [];
    let path = "";
    dfs(left, right, result, path);
    return result;
};
function dfs(left, right, result, path) {
    if(left == 0 && right == 0) {
        result.push(path)
    }
    if(left > 0) {
        dfs(left - 1, right, result, path + "(")
    }
    if(left < right) {
         dfs(left, right - 1, result, path + ")")
    }
}
```

### 4. [557. 反转字符串中的单词 III](https://leetcode-cn.com/problems/reverse-words-in-a-string-iii/)

给定一个字符串，你需要反转字符串中每个单词的字符顺序，同时仍保留空格和单词的初始顺序。



示例：

```
输入："Let's take LeetCode contest"
输出："s'teL ekat edoCteeL tsetnoc"
```

### 思路

两种，第一种是利用js api， 

第二种，类似于滑动窗口，窗口大小为遇到第一个空格位置，把窗口中的字符串倒序插入新数组中，然后移动窗口

```
var reverseWords = function(s) {
    let arr = s.split(" ");
    return arr.map(function(item, index) {
       return item.split("").reverse().join("")
    }).join(" ")
};
```



```
/**
 * @param {string} s
 * @return {string}
 */
var reverseWords = function(s) {
    let result = '';
    let i = 0;
    while(i < s.length) {
        let start = i;
        while(i < s.length && s.charAt(i) !== ' ') {
            i++;
        }
        // 注意，是从 i- 1开始的，因为此时指向的一定是空格
        for(let j = i - 1 ; j >= start; j--) {
            result = result + s.charAt(j)
        }
        while(i< s.length && s.charAt(i) == ' ') {
            i++;
            result = result + ' ';
        }
    }
    return result

};
```



### 5. [165. 比较版本号](https://leetcode-cn.com/problems/compare-version-numbers/)

. 字符不代表小数点，而是用于分隔数字序列。

例如，2.5 不是“两个半”，也不是“差一半到三”，而是第二版中的第五个小版本。

你可以假设版本号的每一级的默认修订版号为 0。例如，版本号 3.4 的第一级（大版本）和第二级（小版本）修订号分别为 3 和 4。其第三级和第四级修订号均为 0。

```
示例 1:
输入: version1 = "0.1", version2 = "1.1"
输出: -1

示例 2:
输入: version1 = "1.0.1", version2 = "1"
输出: 1

示例 3:
输入: version1 = "7.5.2.4", version2 = "7.5.3"
输出: -1

示例 4：
输入：version1 = "1.01", version2 = "1.001"
输出：0
解释：忽略前导零，“01” 和 “001” 表示相同的数字 “1”。

示例 5：
输入：version1 = "1.0", version2 = "1.0.0"
输出：0
解释：version1 没有第三级修订号，这意味着它的第三级修订号默认为 “0”。
```


提示：

版本字符串由以点 （.） 分隔的数字字符串组成。这个数字字符串可能有前导零。
版本字符串不以点开始或结束，并且其中不会有两个连续的点。



### 思路

争取一遍遍历，而不是 先遍历两个 字符串，获取 maxlength，其余补零

双指针

```js
/**
 * @param {string} version1
 * @param {string} version2
 * @return {number}
 */
var compareVersion = function(version1, version2) {
    let arr1 = version1.split(".");
    let arr2 = version2.split(".");
    let i = 0;
    while(arr1[i] && arr2[i]) {
        if(arr1[i] - arr2[i] > 0) {
            return 1
        }
        else if(arr1[i] - arr2[i] < 0){
            return -1
        }
        else {
            i++
        }
    }
    if(arr1.length == i && arr2.length == i) {
        return 0
    }
    else if(i < arr1.length) {
        let res = arr1.slice(i)
        return res.every((item)=>  (item - 0) == 0 ) ? 0 : 1
    }
    else if(i < arr2.length) {
        let res = arr2.slice(i)
        return res.every((item)=>  (item - 0) == 0) ? 0 : -1
    }

};
```

### 6. [剑指 Offer 48. 最长不含重复字符的子字符串](https://leetcode-cn.com/problems/zui-chang-bu-han-zhong-fu-zi-fu-de-zi-zi-fu-chuan-lcof/)

请从字符串中找出一个最长的不包含重复字符的子字符串，计算该最长子字符串的长度。

 

```
示例 1:
输入: "abcabcbb"
输出: 3 
解释: 因为无重复字符的最长子串是 "abc"，所以其长度为 3。

示例 2:
输入: "bbbbb"
输出: 1
解释: 因为无重复字符的最长子串是 "b"，所以其长度为 1。

示例 3:
输入: "pwwkew"
输出: 3
解释: 因为无重复字符的最长子串是 "wke"，所以其长度为 3。
     请注意，你的答案必须是 子串 的长度，"pwke" 是一个子序列，不是子串。



```



### 7. [680. 验证回文字符串 Ⅱ](https://leetcode-cn.com/problems/valid-palindrome-ii/)

给定一个非空字符串 s，最多删除一个字符。判断是否能成为回文字符串。

示例 1:

```
输入: "aba"
输出: True
```


示例 2:

```
输入: "abca"
输出: True
```

解释: 你可以删除c字符。

### 思路

当左右两个指针遇到不等的元素时，按照题目最原本的意思，我们处理的方式是删除 左指针指向的字符 或者 右指针指向的字符，判断 剩余的所有字符 是否可以构成回文串。

### 进阶：

我们注意到「初版方案」中，在找到第一个不相等的元素后，删除了不相等的一个元素，判断 剩余的所有字符 是不是回文字符串。这个做法和题目最原本的意思完全一致。是否可以简化呢？

分析发现，在找到不相等的元素时，[0, left) 和 (right, len(s) - 1] 这两部分已经判断过是回文的，因此不用再次判断。只用判断 [left, right] 区间中的字符串，即删除 left 或者 right 指向的元素，剩余的区间 (left, right] 或者 [left, right) 是否为回文串。

若 (left, right] 或者 [left, right) 为回文串，则说明删除了一个字符可以构成回文串。

如题目的示例 2 ，当左右指针遇到了不等元素时，删除 left 或者 right 指向元素后， 我们只用判断 c 或者 b 是否为回文串。由于这两者是回文串，所以总体的字符串 s 删除 left 或者 right 指向元素也可以构成回文串。



### 错误1

错误原因：

“cbbcc”  按照我写的是false 

实际，可能从左边删除，也可能从右边删除

```js
/**
 * @param {string} s
 * @return {boolean}
 */
var validPalindrome = function(s) {
    let i = 0;
    let j = s.length - 1;
    while(i < j) {
        if(s.charAt(i) !== s.charAt(j)) {

            return isHuiWen(s.slice(i+1, s.length - i))
        }
        i++;
        j--;
    }
    return true
};

function isHuiWen(s) {
    console.log(String(s).split("").reverse().join(""), s)
    console.log(String(s).split("").reverse().join("") == s)
    return String(s).split("").reverse().join("") == s;
}
```



### 改后

```js
/**
 * @param {string} s
 * @return {boolean}
 */
var validPalindrome = function(s) {
    let i = 0;
    let j = s.length - 1;
    while(i < j) {
        if(s.charAt(i) !== s.charAt(j)) {
            let result1 = isHuiWen(s.slice(i+1, j+1))
            let result2 = isHuiWen(s.slice(i, j))
            console.log(s.slice(i+1, j+1), s.slice(i, j))
            return result1 || result2;
        }
        i++;
        j--;
    }
    return true
};

function isHuiWen(s) {
    return String(s).split("").reverse().join("") == s;
}
```



### 8. [9. 回文数](https://leetcode-cn.com/problems/palindrome-number/)

判断一个整数是否是回文数。回文数是指正序（从左向右）和倒序（从右向左）读都是一样的整数。

示例 1:

```
输入: 121
输出: true
```


示例 2:

```
输入: -121
输出: false
解释: 从左向右读, 为 -121 。 从右向左读, 为 121- 。因此它不是一个回文数。
```


示例 3:

```
输入: 10
输出: false
解释: 从右向左读, 为 01 。因此它不是一个回文数。
```

### 代码

思路：反转

```js
/**
 * @param {number} x
 * @return {boolean}
 */
var isPalindrome = function(x) {
    if(x < 0) return false
    return String(x).split("").reverse().join("") == x
};
```

